settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: postgres_graphql
  path: .
  commands:
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor